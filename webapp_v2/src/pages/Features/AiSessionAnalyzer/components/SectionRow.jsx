// Graduated to @/components/SectionRow, which ten pages copy by hand. Re-export
// rather than re-point the three call sites, so the move stays one file.
export { default } from '@/components/SectionRow'
