apiVersion: v2
# Sample chart for the Hoop control plane: the gateway image started with
# "hoop start control-plane" (ADR-0013). Deliberately a skeleton, because the
# control plane serves one route today. No Service, Ingress or config Secret
# yet; add each as the routes behind it are ported.
name: hoop-controlplane-chart
description: A Helm chart for the Hoop control plane
type: application
version: 0.0.1
