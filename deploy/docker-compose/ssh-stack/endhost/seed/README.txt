Files in devuser's home on the end-hop.

  customers.csv   contains email addresses, so the mask rule rewrites them
                  on the way out -- through a terminal and through a download
  secrets.env     the path the guardrail fences, by every route
  upload/         writable, for the put/scp/rsync demonstrations
